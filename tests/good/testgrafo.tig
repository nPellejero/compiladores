/* define a recursive function */
let
  /* calculate n! */
  function f (a : int, b : int) : int =
	let
    var d : int := 0
		var e : int := a
		 
		in
			while e > 0
				do (d := d + b;
						e := e - 1); 
			d
		end
in
   f(4, 2)
end
